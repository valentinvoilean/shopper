import CurrenciesComponent from 'js/currencies/currencies.component';

describe('currencies', function () {
  it('should be a function', function () {
    expect(typeof CurrenciesComponent).toBe('function');
  });
});
